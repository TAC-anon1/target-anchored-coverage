# TAC Target Splits

These are generated target support and evaluation subject lists used by the TAC
training configs. Each `tac_10/train_subjects.txt` contains the 10 target
subjects queried by TAC weighted KMeans for final training. The `eval/` folders
contain the validation and test subject lists and are not used for TAC source
selection.
